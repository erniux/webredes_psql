<%= form_with(model: reconocimiento_rede, local: true) do |form| %>
  <% if reconocimiento_rede.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(reconocimiento_rede.errors.count, "error") %> prohibited this reconocimiento_rede from being saved:</h2>

      <ul>
      <% reconocimiento_rede.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :nombre %>
    <%= form.text_field :nombre %>
  </div>

  <div class="field">
    <%= form.label :descripcion %>
    <%= form.text_area :descripcion %>
  </div>

   
  <div class="actions">
    <%= form.submit "Guardar Registro" %>
  </div>
<% end %>
