<%= form_with(model: certificador, local: true) do |form| %>
  <% if certificador.errors.any? %>
      <% certificador.errors.full_messages.each do |message| %>
        <%= js add_gritter(message, title: "Aviso", sticky: false) %>
      <% end %>
  <% end %>

<div class="container-fluid-crud form-group">
  <div class="row">
    <div class="col-md-2">
      <%= form.label :Nombre %>
    </div>
    <div class="col-md-10">
      <%= form.text_field :nombre, size: 80 %>
    </div>
  </div><br>

  <div class="row">
    <div class="col-md-2">
      <%= form.label :Teléfono %>
    </div>
    <div class="col-md-10">
      <%= form.text_field :telefono, size: 10 %>
    </div>
  </div><br>

  <div class="row">
    <div class="col-md-2">
      <%= form.label :correo %>
    </div>
    <div class="col-md-10">
      <%= form.text_field :correo, size: 50 %>
    </div>
  </div>

    <h2>Escuelas Asignadas</h2>
    
    <div>
      <%= form.fields_for :escuelas do |escuela| %>
      <%= render 'escuelas_fields', f: escuela %>
      <% end %>
    </div>
     
  <div class="actions">
    <%= form.submit "Guardar Registro", class: "btn btn-primary btn-lg" %>
  </div>
<% end %>